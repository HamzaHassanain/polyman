# AGENTS.md

Start at [`CLAUDE.md`](./CLAUDE.md). It explains the layout, the components, and what each is for, then points to per-component references in [`instructions/`](./instructions/) — read those only when the current task touches the component they cover.
